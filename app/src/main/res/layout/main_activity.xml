<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#0a0e27">

    <!-- Toolbar Superior com Gradiente -->
    <androidx.appcompat.widget.Toolbar
        android:id="@+id/toolbar"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:background="@drawable/toolbar_gradient"
        android:elevation="8dp"
        android:minHeight="?attr/actionBarSize"
        android:paddingVertical="8dp"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent">

        <RelativeLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:paddingHorizontal="8dp">

            <!-- Botão Home com estilo neon -->
            <ImageButton
                android:id="@+id/btnHome"
                android:layout_width="48dp"
                android:layout_height="48dp"
                android:layout_centerVertical="true"
                android:background="@drawable/btn_neon_circle"
                android:contentDescription="Home"
                android:padding="12dp"
                android:src="@drawable/ic_arrow_home_foreground"
                app:tint="#00ff88" />

            <!-- Botão Model centralizado -->
            <com.google.android.material.button.MaterialButton
                android:id="@+id/btnModel"
                style="@style/Widget.MaterialComponents.Button.OutlinedButton"
                android:layout_width="wrap_content"
                android:layout_height="48dp"
                android:layout_centerInParent="true"
                android:letterSpacing="0.1"
                android:text="GPT-3.5"
                android:textAllCaps="false"
                android:textColor="#00ff88"
                android:textSize="14sp"
                android:textStyle="bold"
                app:cornerRadius="24dp"
                app:strokeColor="#00ff88"
                app:strokeWidth="2dp" />

            <!-- Botão New com destaque -->
            <com.google.android.material.button.MaterialButton
                android:id="@+id/btnNew"
                android:layout_width="wrap_content"
                android:layout_height="48dp"
                android:layout_alignParentEnd="true"
                android:layout_centerVertical="true"
                android:backgroundTint="#00ff88"
                android:letterSpacing="0.1"
                android:text="New"
                android:textAllCaps="false"
                android:textColor="#0a0e27"
                android:textSize="14sp"
                android:textStyle="bold"
                app:cornerRadius="24dp"
                app:icon="@android:drawable/ic_input_add"
                app:iconGravity="textStart"
                app:iconPadding="4dp"
                app:iconTint="#0a0e27" />
        </RelativeLayout>
    </androidx.appcompat.widget.Toolbar>

    <!-- RecyclerView com efeito de borda neon -->
    <androidx.cardview.widget.CardView
        android:id="@+id/chatContainer"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:layout_margin="12dp"
        android:background="@drawable/chat_container_bg"
        app:cardBackgroundColor="#0f1535"
        app:cardCornerRadius="16dp"
        app:cardElevation="0dp"
        app:layout_constraintBottom_toTopOf="@+id/inputContainer"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/toolbar">

        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/recyclerView"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:clipToPadding="false"
            android:overScrollMode="never"
            android:padding="16dp"
            tools:listitem="@layout/item_message" />
    </androidx.cardview.widget.CardView>

    <!-- Container de Input com design moderno -->
    <androidx.cardview.widget.CardView
        android:id="@+id/inputContainer"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_margin="12dp"
        app:cardBackgroundColor="#0f1535"
        app:cardCornerRadius="28dp"
        app:cardElevation="8dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent">

        <RelativeLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="@drawable/input_border_neon"
            android:padding="4dp">

            <!-- Campo de texto estilizado -->
            <EditText
                android:id="@+id/editMessage"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_centerVertical="true"
                android:layout_marginEnd="56dp"
                android:layout_toStartOf="@+id/btnSend"
                android:background="@android:color/transparent"
                android:hint="Type your message..."
                android:imeOptions="actionSend"
                android:inputType="textCapSentences|textMultiLine"
                android:maxLines="5"
                android:minHeight="48dp"
                android:paddingHorizontal="20dp"
                android:paddingVertical="12dp"
                android:textColor="#ffffff"
                android:textColorHint="#6b7394"
                android:textSize="15sp" />

            <!-- Botão de envio com animação -->
            <ImageButton
                android:id="@+id/btnSend"
                android:layout_width="48dp"
                android:layout_height="48dp"
                android:layout_alignParentEnd="true"
                android:layout_centerVertical="true"
                android:layout_marginEnd="4dp"
                android:background="@drawable/btn_send_neon"
                android:contentDescription="Send"
                android:padding="12dp"
                android:src="@android:drawable/ic_menu_send"
                app:tint="#00ff88" />
        </RelativeLayout>
    </androidx.cardview.widget.CardView>

</androidx.constraintlayout.widget.ConstraintLayout>